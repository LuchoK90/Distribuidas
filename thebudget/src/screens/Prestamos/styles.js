import { StyleSheet } from 'react-native';
import { RecipeCard } from '../../AppStyles';

const styles = StyleSheet.create({
  container: RecipeCard.container,
  title: RecipeCard.title,
  elemento: RecipeCard.elemento
});

export default styles;
